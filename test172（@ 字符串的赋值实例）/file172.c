#include<stdio.h>
int main()
{
   // char s[6]="12345";   //对的,数组s里的元素个数要大于后面赋值的字符数
	//char s[6]={'1','2','3','4','5'};
	//char *s="12345";    //对的，与指针的赋值相似
	  char *s;
      s="12345";  //对的，赋值不能再有*，与指针的赋值相似
	//char *s;
	//*s="112345";   //错的，赋值不能再有*，与指针的赋值相似
	printf("%s\n",s);   //s前不再需要*,与指针变量的内容输出不同，这里s是一般的变量
	return 0;
}